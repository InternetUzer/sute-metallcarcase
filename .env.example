# Copy to .env. Never commit the filled .env file.
# Generate: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=
SITE_HOST=preview.example.com
SITE_URL=https://preview.example.com
TRUSTED_HOSTS=preview.example.com
# Keep 0 on a temporary domain. Switch to 1 only for the public launch.
SITE_INDEXABLE=0
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
NOTIFY_EMAIL=metall-karkas.const@bk.ru
